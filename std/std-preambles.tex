\documentclass[titlepage,twoside,11pt]{article}

% Packages
\usepackage[
    left=3cm,
    right=2cm,
    top=2cm,
    bottom=3cm,
    a4paper]
    {geometry}                      % Set page margins
\usepackage{mathptmx}               % Times New Roman font
\usepackage{changepage}             % Adjustwidth environment

\usepackage{txfonts}                % Symbols (e.g. \copyright)

\usepackage{enumitem}               % Lists
\usepackage{etoolbox}
\usepackage{fmtcount}

\usepackage{lipsum}                 % Dummy text, temporary

\usepackage{fancyhdr}
\usepackage{nameref}
\usepackage[hidelinks]{hyperref}

% Styling
\setlength{\parindent}{0pt}
\setlength{\parskip}{1em}
\setlist[itemize]{topsep=0pt, label=\textendash, leftmargin=1em}
\newenvironment{dashlist}
    {\begin{itemize}\parcountoff}
    {\end{itemize}\parcounton}

% Variables for Header & Footer
% use \renewcommand in the document to set the values
\newcommand{\hdrStdVer}{CPPY??}
\newbool{hdrIsDraft}

% for displaying un-numbered sections in the header and toc
\newcommand{\nsection}[1]{%
    \section*{#1 \protect\markboth{#1}{#1}}%
    \addcontentsline{toc}{section}{#1}%
}

\makeatletter
    \def\cleardoublepage{\clearpage%
        \if@twoside
            \ifodd\c@page\else
                \setbool{parnumflag}{false}
                \vspace*{\fill}
                \hfill
                \begin{center}
                This page is intentionally left blank.
                \end{center}
                \vspace{\fill}
                % \thispagestyle{empty}
                \newpage
                \if@twocolumn\hbox{}\newpage\fi
                \setbool{parnumflag}{true}
            \fi
        \fi
    }
\makeatother

% Tools
\newcommand{\inlinenote}{{\bfseries \ttfamily NOTE \quad}}